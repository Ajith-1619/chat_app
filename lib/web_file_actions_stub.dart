Future<bool> saveWebFile(String url, String fileName) async => false;

Future<bool> openWebFileInApp(
  String url,
  String fileName,
  String mimeType,
) async => false;

String webAttachmentUrl(String url, String fileName) => url;
